name: Hello World
on:
    push:
      branches:
        - main
    workflow_dispatch:
jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello World!"
      - name: Download Code
        uses: actions/checkout@v3